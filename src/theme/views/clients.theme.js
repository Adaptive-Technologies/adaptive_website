import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  header: {

  },
  grid: {
    display: 'flex',
    width: '20%',
    justifyItems: 'center'
  },
  logo: {
    maxHeight: '100px',
    maxWidth: '100px',
  },
}))

export default useStyles
